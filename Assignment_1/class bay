class bay

vector size 3

private:
vehicle*

TEST]]
int ct (counter)


cost of repair = id number

if cost is c then the number of times through the loop = c/5 or c/4. (how many hours it takes to get through the loop)

-someone in priority que goes directly to bay if available.

initialize scrapyard with some # of vehicles.
(10)-random in scrap-type doesn't matter

in bay check each part one at a time

ask about time stamp.

highest cost part (takes longest) when done, any other parts that we have parts for is considered done.

warehouse is just parts. populate array randomly (indexed by part#-1)

if part not in warehouse, put it in priority queue
